/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "images.unsplash.com",
      "images.pexels.com",
      "cdn.pixabay.com",
    ],
  },
};

export default nextConfig;
